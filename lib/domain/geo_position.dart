/// A class for determining the user's geolocation and place.
/// [lat] - latitude, [lon] = longitude.
class GeoPosition {
  final double lat;
  final double lon;

  GeoPosition(
    this.lat,
    this.lon,
  );
}
